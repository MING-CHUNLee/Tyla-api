# Copy this file to secrets.yml and fill in the values
# secrets.yml is gitignored — never commit real secrets

development:
  DATABASE_URL: # leave blank to use default SQLite at backend_app/db/store/development.db

test:
  DATABASE_URL: # leave blank to use default SQLite at backend_app/db/store/test.db

production:
  DATABASE_URL: postgres://user:password@host/tyla_production
