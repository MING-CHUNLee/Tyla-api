STEP 1 — Foundation: Domain values + Infrastructure

1-1. 建立目錄骨架：app/domain/values/, app/infrastructure/llm/, app/infrastructure/middleware/, app/application/prompts/builders/, spec/ 對應目錄

1-2. 寫 Domain values：AttackPolicy, RateLimitPolicy, PayloadLimits, RefusalTemplates, GuardResult

1-3. 寫 spec：attack_policy_spec, refusal_templates_spec, payload_limits_spec（驗常數正確）

1-4. 寫 Prompt builders：JudgeSystemPrompt + TutorSystemPrompt（含 truncation）及其 specs

1-5. 寫 Infrastructure LLM：LlmClient router + OpenAiClient + AnthropicClient（WebMock-stubbed spec：auth header / timeout / non-2xx → LlmError）

1-6. 寫 Middleware：KeyScrubber + spec（sk-xxx 被 REDACTED；clean body 不變）；mount 進 config/application.rb

1-7. 執行 bundle exec rake spec — Step 1 全綠

STEP 2 — Core Services: Guard → Rate → Orchestrator → Handle

2-1. 寫 PolicyLoader + spec（tutor-socratic / tutor-guide 成功讀取；unknown mode → ArgumentError）

2-2. 寫 GuardAgent + spec（allowed < 0.7；blocked ≥ 0.7；benign = 1 - attack；LLM 不可用 fail-open + warn；JSON missing keys fail-open）

2-3. 寫 RateLimiter + spec（允許 < limit；Failure[:rate_limited] 在 limit；window 過期重置；thread-safety）

2-4. 寫 TutorOrchestrator + spec（allowed 呼叫 LLM 回 Success；refused 回 Success 不呼叫 LLM；history truncation；file truncation；timeout → Failure[:upstream_timeout]；LLM error → Failure[:upstream_error]；bad mode → Failure[:bad_mode]）

2-5. 跑 DB migration 002（allowed nullable）：bundle exec rake db:migrate

2-6. 寫 HandleTutorChat + spec（missing headers → 401；rate limited；pending row before LLM；row updated after；X-LLM-Key 不出現在 DB；Orchestrator Failure 傳播；structured log 不含 sk-）

2-7. 新增 RATE_LIMITER = Services::RateLimiter.new 到 config/application.rb

2-8. bundle exec rake spec — Step 2 全綠

STEP 3 — API Layer: Contract + Route + Representer + CORS + 整合測試

3-1. 新增 rack-cors 到 Gemfile；bundle install

3-2. 設定 CORS middleware（CORS_ALLOW_ORIGIN env var）及 Origin 白名單 Rack middleware；mount 進 config/application.rb

3-3. 刪除 app/application/requests/create_prompt_log.rb；寫新 TutorChatRequest contract（json block + mode rule + payload-size rules）

3-4. 寫 TutorChatResponseRepresenter（Roar::JSON）

3-5. 改寫 app_routes.rb：pattern-match on Result，加 429 / 401 / 422 / 502 / 504 branch

3-6. 寫整合 specs：prompt_logs_route_spec（422 missing fields / bad mode / oversized；401；429；200 ok；200 refused；502/504；DB row 只寫一次；X-LLM-Key 不出現 response body）；header_access_spec（pin request_headers plugin）

3-7. bundle exec rake spec — 全部 specs 全綠

3-8. Smoke test：對 frontend refactor/thin-client branch 端到端驗證 ok / refused 兩條路徑後合併